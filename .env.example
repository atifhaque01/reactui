# Copy this file to ".env" and adjust, or set as real environment variables
# before building/starting the app. Create React App only exposes variables
# prefixed with REACT_APP_ to the browser.

# Base URL of the backend API
API_BASE_URL=http://localhost:3012
