// Base URL for the backend API. Override by setting REACT_APP_API_BASE_URL
// in your environment (e.g. a .env file) before building/starting the app.
export const API_BASE_URL =
  process.env.REACT_APP_API_BASE_URL || "http://localhost:3012";
